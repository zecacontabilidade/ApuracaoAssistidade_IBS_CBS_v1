# Pacote de testes do motor fiscal IBS/CBS (fiscal_engine).
# Gate de cobertura: fiscal_engine >= 95% (CLAUDE.md / ADR 0008).
# Fixtures SINTÉTICAS apenas — NUNCA dados reais de cliente (LGPD / CLAUDE.md).
