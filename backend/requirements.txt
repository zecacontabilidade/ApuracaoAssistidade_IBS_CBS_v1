# SHIM — Este arquivo é mantido apenas para compatibilidade com ferramentas que
# não leem pyproject.toml diretamente (ex.: scanners de segurança, Dependabot,
# ambientes que esperam um requirements.txt).
#
# FONTE DE VERDADE: backend/pyproject.toml
#   [project.dependencies]            — dependências de runtime
#   [project.optional-dependencies]   — extras dev e test
#
# Instalação correta (a partir de /workspace):
#   pip install -e "backend[dev,test]"   ← dev + test + runtime
#   pip install -e "backend"             ← apenas runtime
#
# NÃO edite este arquivo diretamente. Atualize backend/pyproject.toml.
